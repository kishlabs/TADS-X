# configs/train_config.yaml — updated with overfitting fixes
epochs_phase1:       20
epochs_phase2:       25
batch_size:          32
learning_rate:       0.0001
optimizer:           AdamW
weight_decay: 0.005
lambda_contrastive:  0.2
tau:                 0.07
device:              cuda
seed:                42
val_every_n_epochs:  1
checkpoint_every:    5
checkpoint_dir:      checkpoints
out_dir:             data
max_proposals:       8
prune_thresh:        0.01
yolo_conf:           0.25
yolo_weights:        yolov8n.pt